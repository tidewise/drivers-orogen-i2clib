# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(i2clibTaskLib)
ADD_LIBRARY(${I2CLIB_TASKLIB_NAME} SHARED
    ${I2CLIB_TASKLIB_SOURCES})
add_dependencies(${I2CLIB_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${I2CLIB_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${I2CLIB_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${I2CLIB_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${I2CLIB_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${I2CLIB_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${I2CLIB_TASKLIB_INTERFACE_LIBRARIES}")


INSTALL(TARGETS ${I2CLIB_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${I2CLIB_TASKLIB_HEADERS}
    DESTINATION include/orocos/i2clib)

